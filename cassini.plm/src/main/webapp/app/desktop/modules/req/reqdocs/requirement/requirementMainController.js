define(
    [
        'app/desktop/modules/req/reqdocs/requirement/requirement.module'
    ],
    function (module) {
        module.controller('RequirementMainController', RequirementMainController);

        function RequirementMainController($scope, $rootScope, $timeout, $state, $stateParams, $cookies) {
            var vm = this;

            (function () {

            })();
        }
    }
);