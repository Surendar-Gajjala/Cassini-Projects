//Define custom code that should be executed on application start here (e.g. event listener on the global vsClient object)

//An example for an event listener on the vsClient object:
/*
vsClient.on('NodePropertiesChanged', res =>
{
    console.log(res);
});
*/